# 📋 الخطة التنفيذية المعتمدة والنهائية: الهيكل التنظيمي المطور (SaaS)

تم تطوير هذه الخطة بالكامل لتلبي متطلبات الهيكل التنظيمي للمجموعات والشركات السعودية (الكيانات الكبرى بالفروع التابعة والمشاريع وتوزيع الموظفين)، مع دمج تحسينات الجودة لمنع أخطاء الإدخال وحماية سلامة البيانات.

---

## 1. الهيكل التنظيمي للمجموعة (Organizational Structure)

1. **الكيانات الكبرى (Legal Entities):**
   * الكيانات القانونية الرئيسية للمجموعة (مثال: شركة ريال البركة للتجارة).
   * الحقول: المعرف (ID)، الاسم (Name)، **الرقم الموحد 700** (Unified Number).

2. **الفروع (Branches):**
   * الفروع التشغيلية التابعة للكيانات الكبرى (مثال: فرع الرياض، فرع جدة).
   * الحقول: المعرف (ID)، الاسم (Name)، **رقم السجل التجاري** (CR Number)، **الكيان الكبير التابع له** (Entity ID).

3. **المشاريع (Projects):**
   * يتبع كل مشروع لـ "فرع" معين أو "كيان كبير" معين.
   * الحقول: المعرف (ID)، الاسم (Name)، معرف الكيان التابع له (Entity ID - اختياري)، معرف الفرع التابع له (Branch ID - اختياري).

4. **الموظفون (Employees):**
   * **الفرع القانوني (Legal Branch):** الفرع المسجل عليه الموظف قانونياً (لحساب التأمينات والتوطين للكيان الكبير التابع له).
   * **الفرع المالي (Cost Branch):** الفرع الذي يتحمل تكلفة وراتب الموظف فعلياً (في تقارير المشاريع والفروع المجمعة).
   * **المشروع المنسب له (Project):** المشروع المرتبط بالفرع المالي/القانوني للموظف.
   * **نوع الموظف السعودي:** (سعودي عامل / سعودة لدعم النشاط فقط).

---

## 2. تحسينات الجودة وحماية البيانات المعتمدة (Quality & Safety Enhancements)

1. **حساب السعودة التلقائي 100%:**
   * إلغاء مبالغ السعودة اليدوية. يتم حساب السعودة لكل كيان كبير تلقائياً بجمع تكاليف موظفيه السعوديين القانونيين (الراتب الأساسي + البدلات + التأمينات 22%).

2. **تحديث ملف استيراد الإكسيل (Excel Import):**
   * تعديل مستورد ملفات الإكسيل ليدعم الهيكل الجديد (الفرع القانوني، الفرع المالي، نوع السعودي، والمشروع) وربطها تلقائياً.

3. **حماية الحذف التراكمي (Cascading Delete Protection):**
   * منع حذف الكيانات الكبرى التي تتبعها فروع نشطة.
   * منع حذف الفروع التي تحتوي على موظفين أو مشاريع نشطة لتلافي أخطاء وتلف قاعدة البيانات.

4. **الفلترة الديناميكية لخيارات المشاريع:**
   * في نموذج الموظف، بمجرد اختيار الفرع المالي، يتم فلترة قائمة المشاريع المتاحة لتظهر فقط المشاريع المرتبطة بالفرع المختار لمنع أخطاء الإدخال.

5. **تقسيم لوحة التحكم (Compliance vs Financial View):**
   * **جدول الالتزام القانوني:** يعرض الكيانات الكبرى ونسب توطينها وعبء السعودة الناتج عنها.
   * **جدول التشغيل المالي:** يعرض الفروع وتكاليف الرواتب والمخصصات الفعلية المحملة عليها.

---

## 3. هيكل قاعدة البيانات المحدث (Database Schema)

* **جدول الكيانات الكبرى (`entities`):**
  * `id` INTEGER PRIMARY KEY, `name` TEXT, `unified_number` TEXT
* **جدول الفروع الجديد (`branches`):**
  * `id` INTEGER PRIMARY KEY, `name` TEXT, `cr_number` TEXT, `entity_id` INTEGER
* **جدول المشاريع المحدث (`projects`):**
  * `id` INTEGER PRIMARY KEY, `name` TEXT, `entity_id` INTEGER, `branch_id` INTEGER
* **جدول الموظفين (`employees`):**
  * `branch_id` INTEGER (الفرع القانوني)
  * `cost_branch_id` INTEGER (الفرع المالي)
  * `project_id` INTEGER (المشروع)
  * `saudi_type` TEXT (نوع التوطين: `'working'` أو `'support'`)

---

## 4. خطة التحديث والتنفيذ (Execution Phases)

* **المرحلة 1:** تحديث كود [db.js](file:///c:/Users/z2k1s/Music/12522/backend/db.js) و[server.js](file:///c:/Users/z2k1s/Music/12522/backend/server.js) لدعم جداول الفروع والمشاريع الجديدة والحماية والتحقق من الحذف.
* **المرحلة 2:** تحديث كود [costEngine.js](file:///c:/Users/z2k1s/Music/12522/backend/costEngine.js) لحساب عبء السعودة آلياً وربط التكاليف بالفروع المالية.
* **المرحلة 3:** تحديث الواجهة الأمامية بالكامل (شاشات لوحة التحكم، الموظفين، الإعدادات، واستيراد الإكسل).
